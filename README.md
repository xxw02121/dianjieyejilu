# 水系锌电实验记录系统

专为水系锌离子电池研究设计的实验配方与结果记录管理系统。

## 📋 功能特性

- ✅ **结构化记录** - DES 电解液和水凝胶配方的专业模板
- ✅ **便捷分享** - 一键生成公网分享链接
- ✅ **数据安全** - 基于 Supabase 的权限管理
- ✅ **多设备同步** - 云端数据随时访问

## 🚀 快速开始

### 第一步：安装依赖

打开命令行（PowerShell），进入项目目录：

```bash
cd C:\Users\Administrator\Desktop\1\zn-battery-lab
npm install
```

**等待安装完成**（大约 2-3 分钟），看到 `added XXX packages` 表示成功。

### 第二步：配置环境变量

1. 复制 `.env.example` 文件并重命名为 `.env.local`
2. 打开 `.env.local` 文件
3. 填入你的 Supabase 配置（从 Supabase Dashboard > Settings > API 获取）：

```env
NEXT_PUBLIC_SUPABASE_URL=https://你的项目ID.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=你的匿名密钥
```

### 第三步：配置 Supabase 数据库

1. 访问 [Supabase](https://supabase.com) 并登录
2. 创建新项目（选择新加坡或东京节点）
3. 打开 **SQL Editor**，执行以下 SQL（已包含在项目中）：

参考文档：`docs/database-setup.sql`

### 第四步：本地运行

```bash
npm run dev
```

打开浏览器访问：`http://localhost:3000`

**✅ 成功标志**：看到中文首页，显示"水系锌电实验记录系统"

### 第五步：测试功能

1. 点击"注册"创建账号
2. 填写邮箱和密码（密码至少 6 位）
3. 检查邮箱，点击确认链接
4. 登录系统
5. 点击"新建记录"创建第一条实验记录

## 📦 部署到 Vercel（公网访问）

### 方法一：通过 Vercel 网站

1. 访问 [Vercel](https://vercel.com)
2. 使用 GitHub 登录
3. 点击 **"Add New" → "Project"**
4. 导入你的 GitHub 仓库（需先将代码推送到 GitHub）
5. 配置环境变量：

   - `NEXT_PUBLIC_SUPABASE_URL` = 你的 Supabase URL
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` = 你的 Anon Key

6. 点击 **"Deploy"**
7. 等待 2-3 分钟，部署成功后获得公网链接

### 方法二：通过命令行

```bash
# 安装 Vercel CLI
npm install -g vercel

# 登录
vercel login

# 部署
vercel

# 生产部署
vercel --prod
```

## 🔧 故障排查

### 问题 1：npm install 失败

**现象**：出现 `ERR!` 或超时

**解决**：
```bash
# 清除缓存
npm cache clean --force

# 重新安装
npm install
```

### 问题 2：localhost:3000 无法访问

**现象**：浏览器显示"无法访问此网站"

**解决**：
1. 确认命令行显示 `Local: http://localhost:3000`
2. 检查是否有其他程序占用 3000 端口
3. 尝试访问 `http://127.0.0.1:3000`

### 问题 3：登录后显示错误

**现象**：注册/登录时提示错误

**解决**：
1. 检查 `.env.local` 文件是否正确配置
2. 确认 Supabase 数据库表已创建
3. 在 Supabase Dashboard > Authentication > Settings 中启用 Email 登录

### 问题 4：部署到 Vercel 后报错

**现象**：Vercel 部署成功但访问报错

**解决**：
1. 在 Vercel Dashboard > Settings > Environment Variables 中添加环境变量
2. 重新部署（Deployments > 最新部署 > Redeploy）

## 📚 项目结构

```
zn-battery-lab/
├── app/                    # Next.js App Router 页面
│   ├── login/             # 登录页
│   ├── register/          # 注册页
│   ├── dashboard/         # 仪表盘
│   └── records/           # 实验记录
│       ├── new/           # 新建记录
│       └── [id]/          # 记录详情
├── components/            # React 组件
│   └── ui/                # UI 组件库
├── lib/                   # 工具函数
│   ├── supabase/          # Supabase 客户端
│   └── utils.ts           # 通用工具
├── .env.local             # 环境变量（需手动创建）
└── package.json           # 项目依赖
```

## 🎯 下一步

- [ ] 添加附件上传功能
- [ ] 实现分享链接生成
- [ ] 支持 PDF 导出
- [ ] 添加数据统计图表

## 📞 获取帮助

如果遇到任何问题，请提供：

1. 错误截图
2. 命令行输出（复制文本）
3. 操作步骤描述

我将帮你逐步解决！
